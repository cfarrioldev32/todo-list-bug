Mejoras implementadas:

1. Ya no se puede acceder a la lista de tareas o al detalle de una tarea si no eres el dueño
2. Solo pueden editar las tareas los dueños de las mismas
3. Se implemento un guard global para proteger las rutas que no sean publicas
4. Se mejora y se expande el maejo de los errores
5. se implementó el crear una tarea y asi se completa el crud
6. utilice git flow como standar para los commits y los pr